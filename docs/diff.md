# `dusk diff`

Colorized side-by-side git diff with line numbers.

## Usage

```bash
dusk diff [theme] [--staged]
```

Requires `git` in `PATH`.

## Flags

- `--staged`: diff staged changes
- `--help`, `-h`: show help

## Examples

```bash
dusk diff
dusk diff --staged
dusk diff monokai
```
